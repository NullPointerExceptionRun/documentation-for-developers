/* 
Vamos falar um pouco sobre variáveis em JavaScript. 
Variável como o nome já diz, é algo que pode ser mutavel e atribuido, em JavaScript utilizamos variáveis para atribuir valores que queremos de alguma forma utilizarmos mais a frente.
*/

// Existem três tipos de variáveis

var nome = "Wesley"; // A variável do tipo string, que nada mais é do que um texto delimitado pelas aspas duplas("")

var idade = 21; // A variável do tipo number, que são números.

var maiorDeIdade = true; // A variável do tipo bolean, que recebe os atributos "true" e "false".
